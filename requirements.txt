# =============================================================================
# backend/requirements.txt
# Flask Video Downloader — Production Dependencies
# =============================================================================
# Install:  pip install -r requirements.txt
# Requires: Python 3.12+
# Deploy:   Render (gunicorn), local dev (flask run / python app.py)
#
# NOTE: FFmpeg is a system binary, NOT a Python package.
#   • Render:   installed via buildCommand in render.yaml
#   • macOS:    brew install ffmpeg
#   • Linux:    apt-get install ffmpeg
# =============================================================================

# -----------------------------------------------------------------------------
# 1. Web Framework
# -----------------------------------------------------------------------------
Flask==3.1.1
flask-cors==5.0.1

# -----------------------------------------------------------------------------
# 2. Video Download Engine
#    yt-dlp — use --pre to get the latest nightly with YouTube fixes.
#    YouTube frequently changes their API; the nightly builds include
#    up-to-date extractor patches (PO token handling, player_client fixes).
#
#    IMPORTANT: On Render, the buildCommand uses:
#      pip install -U --pre "yt-dlp[default]"
#    to always pull the latest nightly. The version below is a minimum floor.
# -----------------------------------------------------------------------------
yt-dlp>=2025.06.30

# -----------------------------------------------------------------------------
# 3. Production WSGI Server
# -----------------------------------------------------------------------------
gunicorn==23.0.0

# -----------------------------------------------------------------------------
# 4. Environment Variable Management
# -----------------------------------------------------------------------------
python-dotenv==1.0.1

# -----------------------------------------------------------------------------
# 5. SSL / TLS Certificate Bundle
# -----------------------------------------------------------------------------
certifi==2025.4.26

# -----------------------------------------------------------------------------
# 6. API Rate Limiting (Security)
# -----------------------------------------------------------------------------
Flask-Limiter==3.7.0
limits==3.13.0

# -----------------------------------------------------------------------------
# 7. Async Worker for Gunicorn (gevent for SSE streaming)
# -----------------------------------------------------------------------------
gevent==24.2.1
