# =============================================================================
# backend/requirements.txt
# Flask Video Downloader — Production Dependencies
# =============================================================================
# Install:  pip install -r requirements.txt
# Requires: Python 3.12+
# Deploy:   Render (gunicorn), local dev (flask run / python app.py)
#
# SYSTEM DEPENDENCIES (installed via render.yaml buildCommand):
#   • FFmpeg:  apt-get install -y ffmpeg
#   • Node.js: apt-get install -y nodejs npm
#
# yt-dlp 2025+ REQUIRES Node.js for YouTube's n-parameter challenge.
# Without Node.js you get: TypeError: 'NoneType' object is not callable
# =============================================================================

# --- Web Framework ---
Flask==3.1.1
flask-cors==5.0.1

# --- Video Download Engine ---
# The [default] extra pulls in optional deps (brotli, websockets, etc.)
# render.yaml buildCommand also runs: pip install -U --pre "yt-dlp[default]"
# to get the latest nightly with YouTube extractor fixes.
yt-dlp[default]>=2025.06.30

# --- Production WSGI Server ---
gunicorn==23.0.0

# --- Environment Variable Management ---
python-dotenv==1.0.1

# --- SSL Certificate Bundle ---
certifi==2025.4.26

# --- API Rate Limiting ---
Flask-Limiter==3.7.0
limits==3.13.0

# --- Async Worker for Gunicorn (gevent for SSE streaming) ---
gevent==24.2.1
